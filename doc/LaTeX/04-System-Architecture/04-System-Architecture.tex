\documentclass[12pt,a4paper]{article}

\usepackage[margin=2.2cm]{geometry}
\usepackage{fontspec}
\usepackage{xepersian}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{tikz}
\usepackage{adjustbox}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.multipart, fit, calc}

\providecommand{\UseMathForPositioningText}{}

\IfFontExistsTF{Vazirmatn}{
  \settextfont{Vazirmatn}
}{
  \IfFontExistsTF{Tahoma}{
    \settextfont{Tahoma}
  }{
    \settextfont{Arial}
  }
}
\IfFontExistsTF{Arial}{
  \setlatintextfont{Arial}
}{
  \setlatintextfont{Times New Roman}
}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  pdftitle={AI Chat SaaS System Architecture},
  pdfauthor={Mobin Yousefi}
}

\definecolor{HeaderBlue}{HTML}{1E3A8A}
\definecolor{AppBlue}{HTML}{DBEAFE}
\definecolor{ApiGreen}{HTML}{DCFCE7}
\definecolor{DataYellow}{HTML}{FEF3C7}
\definecolor{RiskRed}{HTML}{FEE2E2}
\definecolor{SoftGray}{HTML}{F3F4F6}
\definecolor{BorderGray}{HTML}{94A3B8}

\setlength{\parindent}{0pt}
\setlength{\parskip}{7pt}
\setlength{\headheight}{15pt}
\emergencystretch=3em
\sloppy
\renewcommand{\arraystretch}{1.45}
\setlist[itemize]{rightmargin=1.2cm,itemsep=3pt,topsep=4pt}
\setlist[enumerate]{rightmargin=1.2cm,itemsep=3pt,topsep=4pt}

\titleformat{\section}{\Large\bfseries\color{HeaderBlue}}{\thesection}{0.6em}{}
\titleformat{\subsection}{\large\bfseries}{\thesubsection}{0.6em}{}
\titleformat{\subsubsection}{\normalsize\bfseries}{\thesubsubsection}{0.6em}{}

\pagestyle{fancy}
\fancyhf{}
\rhead{مستند معماری سیستم AI Chat SaaS}
\lhead{نسخه 1.0}
\cfoot{\thepage}

\newcommand{\en}[1]{\lr{#1}}
\newcommand{\code}[1]{\lr{\texttt{#1}}}

\tikzset{
  box/.style={rectangle, rounded corners=3pt, draw=BorderGray, thick, align=center, minimum height=1cm, text width=3.1cm, font=\small},
  smallbox/.style={rectangle, rounded corners=3pt, draw=BorderGray, thick, align=center, minimum height=0.85cm, text width=2.5cm, font=\scriptsize},
  actor/.style={rectangle, rounded corners=10pt, draw=HeaderBlue, thick, fill=AppBlue, align=center, minimum height=0.9cm, text width=2.6cm, font=\small},
  api/.style={box, fill=ApiGreen},
  app/.style={box, fill=AppBlue},
  data/.style={box, fill=DataYellow},
  risk/.style={box, fill=RiskRed},
  line/.style={-Latex, thick, draw=HeaderBlue},
  dashedline/.style={-Latex, thick, dashed, draw=BorderGray}
}

\begin{document}

\begin{titlepage}
\centering
\vspace*{2.2cm}
{\Huge\bfseries مستند معماری سیستم \en{AI Chat SaaS}\par}
\vspace{0.7cm}
{\Large\bfseries \en{System Architecture Documentation}\par}
\vspace{1.3cm}
{\large معماری سامانه چندمستاجری چت پشتیبانی، پنل مدیریتی، ویجت سایت و دستیار هوشمند مبتنی بر دانش\par}
\vfill
\begin{tabular}{rl}
\textbf{نام پروژه:} & \en{AI Chat SaaS} \\
\textbf{نوع سند:} & مستند معماری سیستم \\
\textbf{نسخه سند:} & 1.0 \\
\textbf{تاریخ:} & ۱۴ جولای ۲۰۲۶ \\
\textbf{نویسنده/تهیه‌کننده:} & مبین یوسفی \\
\textbf{مسیر سند:} & \code{doc/LaTeX/04-System-Architecture/04-System-Architecture.tex} \\
\end{tabular}
\vfill
{\small این سند برای ارائه معماری فنی، اجزای سیستم، جریان ارتباطی، مدل استقرار، مدل داده و مرزهای امنیتی سامانه تهیه شده است.}
\end{titlepage}

\tableofcontents
\newpage

\section{هدف و دامنه سند}
این سند معماری سیستم \en{AI Chat SaaS} را تشریح می‌کند. تمرکز سند بر نمایش ساختار واقعی محصول، اجزای نرم‌افزاری، ارتباط بین لایه‌ها، جریان‌های اصلی، مدل داده، استقرار، شبکه، امنیت و تصمیم‌های معماری است.

مطابق راهنمای \code{Rahnama.md}، این سند شامل موارد زیر است:
\begin{itemize}
  \item \en{Architecture Diagram}
  \item \en{Context Diagram}
  \item \en{Component Diagram}
  \item \en{Deployment Diagram}
  \item \en{Sequence Diagram}
  \item \en{Class/Domain Diagram}
  \item \en{Microservice Diagram} به‌صورت نقشه ماژول‌های سرویس‌مانند در معماری فعلی
  \item \en{Network Diagram}
  \item \en{Database Diagram}
\end{itemize}

منابع فنی این سند شامل فایل‌های \code{ArchitectureForMobin.md}، \code{ReportForSale.md}، endpointهای \code{backend/api}، helperهای \code{backend/includes}، migration مربوط به AI و ساختار frontend و widget است.

\section{خلاصه معماری}
\en{AI Chat SaaS} یک سامانه B2B SaaS چندمستاجری است که از سه سطح اصلی تشکیل شده است:
\begin{enumerate}
  \item \textbf{سطح عمومی سایت مشتری:} ویجت جاوااسکریپت که روی سایت مشتری نصب می‌شود و با API عمومی widget ارتباط دارد.
  \item \textbf{سطح عملیات پشتیبانی:} پنل Next.js برای سوپرادمین، ادمین مشتری و agentها.
  \item \textbf{سطح بک‌اند و داده:} APIهای PHP، پایگاه داده MySQL، فضای upload و موتور دانش/AI.
\end{enumerate}

بک‌اند در نسخه فعلی به‌صورت endpointهای مستقل PHP طراحی شده است. هر endpoint مسئول یک عملیات مشخص است و قابلیت‌های مشترک مانند احراز هویت، پاسخ JSON، CORS، کنترل دسترسی سایت، rate limit، upload و موتور AI از طریق فایل‌های \code{backend/includes} استفاده می‌شوند.

\section{Architecture Diagram}
\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=1.4cm and 1.6cm]
  \node[actor] (visitor) {بازدیدکننده\\\en{Website Visitor}};
  \node[actor, below=of visitor] (agent) {ادمین/اپراتور\\\en{Admin / Agent}};
  \node[actor, below=of agent] (super) {سوپرادمین\\\en{Super Admin}};

  \node[app, right=2.2cm of visitor] (widget) {ویجت چت\\\code{widget/dist/widget.js}\\\en{Shadow DOM}};
  \node[app, right=2.2cm of agent] (panel) {پنل مدیریتی\\\code{frontend}\\\en{Next.js / React}};

  \node[api, right=2.4cm of widget] (widgetapi) {API عمومی ویجت\\\code{backend/api/widget}};
  \node[api, right=2.4cm of panel] (panelapi) {API پنل\\\code{auth / agent / customer / super-admin}};

  \node[api, right=2.4cm of widgetapi] (core) {هسته بک‌اند\\\code{includes}\\Auth, Access, Rate Limit, Upload};
  \node[data, above right=0.6cm and 2.1cm of core] (db) {پایگاه داده\\\en{MySQL / InnoDB}};
  \node[data, right=2.1cm of core] (ai) {موتور دانش و AI\\Crawler, Search, Scoring};
  \node[data, below right=0.6cm and 2.1cm of core] (storage) {فضای فایل\\Uploads / Attachments};

  \draw[line] (visitor) -- (widget);
  \draw[line] (agent) -- (panel);
  \draw[line] (super) |- (panel);
  \draw[line] (widget) -- (widgetapi);
  \draw[line] (panel) -- (panelapi);
  \draw[line] (widgetapi) -- (core);
  \draw[line] (panelapi) -- (core);
  \draw[line] (core) -- (db);
  \draw[line] (core) -- (ai);
  \draw[line] (core) -- (storage);
  \draw[dashedline] (ai) -- (db);
\end{tikzpicture}
\end{adjustbox}
\caption{نمای معماری کلان سامانه}
\end{figure}

\subsection{توضیح معماری کلان}
در این معماری، ویجت و پنل دو client مستقل هستند. ویجت از طریق \code{site\_key} و کنترل origin به API عمومی وصل می‌شود. پنل مدیریتی با JWT و نقش کاربر به endpointهای محافظت‌شده متصل می‌شود. بک‌اند، مرز اصلی اعمال قوانین دسترسی، محدودیت پلن، امنیت فایل، ثبت مکالمه و اجرای منطق AI است.

\section{Context Diagram}
\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=1.5cm and 2cm]
  \node[api, text width=4.4cm, minimum height=2cm] (system) {سامانه \en{AI Chat SaaS}\\چت، پشتیبانی، AI، SaaS Admin};
  \node[actor, above left=of system] (customerSite) {سایت مشتری};
  \node[actor, left=of system] (visitor) {بازدیدکننده};
  \node[actor, below left=of system] (admin) {ادمین مشتری / Agent};
  \node[actor, below=of system] (super) {مالک پلتفرم};
  \node[data, right=of system] (mysql) {MySQL};
  \node[data, above right=of system] (webpages) {صفحات سایت مشتری\\برای Crawl};
  \node[data, below right=of system] (uploads) {Storage فایل‌ها};

  \draw[line] (customerSite) -- node[above, font=\scriptsize]{نصب ویجت} (system);
  \draw[line] (visitor) -- node[above, font=\scriptsize]{پیام/فایل} (system);
  \draw[line] (admin) -- node[below, font=\scriptsize]{مدیریت مکالمه و دانش} (system);
  \draw[line] (super) -- node[right, font=\scriptsize]{مدیریت SaaS} (system);
  \draw[line] (system) -- node[above, font=\scriptsize]{CRUD / Query} (mysql);
  \draw[dashedline] (system) -- node[above, font=\scriptsize]{Fetch HTML} (webpages);
  \draw[line] (system) -- node[below, font=\scriptsize]{Upload / Download URL} (uploads);
\end{tikzpicture}
\end{adjustbox}
\caption{نمودار Context و مرز سامانه}
\end{figure}

\section{Component Diagram}
\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=1.1cm and 1.2cm]
  \node[app] (frontend) {پنل Next.js\\Pages, Components\\\code{frontend/lib/api.ts}};
  \node[app, below=of frontend] (widget) {ویجت JavaScript\\Shadow DOM\\Polling, LocalStorage};

  \node[api, right=of frontend] (auth) {Auth API\\Login, Me, Password};
  \node[api, below=of auth] (agentapi) {Agent API\\Conversations, Messages\\AI Suggestions};
  \node[api, below=of agentapi] (customerapi) {Customer API\\Sites, Team, Knowledge\\AI Settings};
  \node[api, below=of customerapi] (superapi) {Super Admin API\\Tenants, Plans, Sites};
  \node[api, below=of superapi] (widgetapi) {Widget API\\Config, Visitor\\Conversation, AI Reply};

  \node[api, right=1.5cm of agentapi] (shared) {Shared Includes\\Auth, JWT, Response\\CORS, Upload, Rate Limit\\Site Access, Plan Limits};
  \node[api, right=1.5cm of customerapi] (ai) {AI Knowledge Engine\\Crawler, Chunking\\Terms, Scoring};
  \node[data, right=1.7cm of shared] (db) {MySQL Database\\Tenants, Sites, Users\\Conversations, Messages\\AI Tables};
  \node[data, below=of db] (files) {Upload Storage\\Chat Attachments\\Announcement Images};

  \draw[line] (frontend) -- (auth);
  \draw[line] (frontend) -- (agentapi);
  \draw[line] (frontend) -- (customerapi);
  \draw[line] (frontend) -- (superapi);
  \draw[line] (widget) -- (widgetapi);

  \draw[line] (auth) -- (shared);
  \draw[line] (agentapi) -- (shared);
  \draw[line] (customerapi) -- (shared);
  \draw[line] (superapi) -- (shared);
  \draw[line] (widgetapi) -- (shared);
  \draw[line] (agentapi) -- (ai);
  \draw[line] (customerapi) -- (ai);
  \draw[line] (widgetapi) -- (ai);
  \draw[line] (shared) -- (db);
  \draw[line] (ai) -- (db);
  \draw[line] (shared) -- (files);
\end{tikzpicture}
\end{adjustbox}
\caption{نمودار اجزای نرم‌افزاری}
\end{figure}

\subsection{اجزای اصلی}
\begin{longtable}{>{\raggedleft\arraybackslash}p{4cm} p{10.5cm}}
\toprule
\textbf{Component} & \textbf{مسئولیت} \\
\midrule
\code{frontend} & رابط مدیریتی برای نقش‌های super admin، customer admin و agent. ارتباط با API از طریق \code{frontend/lib/api.ts}. \\
\code{widget} & ویجت قابل نصب روی سایت مشتری؛ مدیریت visitor، conversation، ارسال پیام، پیوست، نمایش پیام‌ها و درخواست پاسخ AI. \\
\code{backend/api/auth} & ورود، دریافت کاربر جاری، تغییر رمز و خروج از همه دستگاه‌ها. \\
\code{backend/api/widget} & API عمومی با کنترل \code{site\_key}، origin، tenant/site status و rate limit. \\
\code{backend/api/agent} & API عملیات پشتیبانی، مکالمات، پیام‌ها، presence، typing و پیشنهاد AI. \\
\code{backend/api/customer} & مدیریت سایت‌ها، تنظیمات AI، دانش، crawl، team، reports و plan usage. \\
\code{backend/api/super-admin} & مدیریت کل SaaS شامل مشتریان، پلن‌ها، سایت‌ها، کاربران و اعلان‌ها. \\
\code{backend/includes} & هسته مشترک شامل auth، JWT، response، CORS، site access، plan limits، upload، rate limit و AI. \\
\code{MySQL} & نگهداری داده‌های tenant، site، user، visitor، conversation، message، plan، knowledge و AI. \\
\bottomrule
\end{longtable}

\section{Deployment Diagram}
\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=1.25cm and 1.5cm]
  \node[actor] (browser1) {Browser\\Visitor};
  \node[actor, below=of browser1] (browser2) {Browser\\Admin/Agent};
  \node[app, right=of browser1] (cdn) {Static Hosting\\\code{widget/dist/widget.js}};
  \node[app, right=of browser2] (next) {Frontend Server\\Next.js\\Port 3000 / HTTPS};
  \node[api, right=1.8cm of next] (php) {PHP Web Server\\Apache/Nginx + PHP\\\code{backend/api}};
  \node[data, right=1.8cm of php] (mysql) {MySQL Server\\Database\\\code{ai\_chat\_saas}};
  \node[data, below=of mysql] (storage) {Filesystem Storage\\\code{backend/storage/uploads}};
  \node[data, above=of php] (env) {Environment Config\\.env, URLs, JWT, CORS};

  \draw[line] (browser1) -- node[above, font=\scriptsize]{load script} (cdn);
  \draw[line] (browser1) |- node[above, font=\scriptsize]{HTTPS JSON API} (php);
  \draw[line] (browser2) -- node[below, font=\scriptsize]{HTTPS} (next);
  \draw[line] (next) -- node[above, font=\scriptsize]{fetch API + JWT} (php);
  \draw[line] (php) -- node[above, font=\scriptsize]{PDO} (mysql);
  \draw[line] (php) -- node[right, font=\scriptsize]{read/write files} (storage);
  \draw[dashedline] (env) -- (php);
\end{tikzpicture}
\end{adjustbox}
\caption{نمودار استقرار منطقی}
\end{figure}

\subsection{واحدهای قابل استقرار}
\begin{itemize}
  \item \textbf{Frontend:} پروژه Next.js که می‌تواند روی Node.js server یا سرویس hosting مناسب Next.js اجرا شود.
  \item \textbf{Backend:} فایل‌های PHP زیر وب‌سرور Apache/Nginx با PHP و دسترسی به MySQL.
  \item \textbf{Widget:} فایل جاوااسکریپت static که بهتر است از CDN یا مسیر public پایدار ارائه شود.
  \item \textbf{Database:} MySQL با charset مناسب \code{utf8mb4} برای داده‌های فارسی.
  \item \textbf{Storage:} مسیر ذخیره فایل‌های پیوست و تصاویر اعلان‌ها.
\end{itemize}

\section{Sequence Diagramها}
\subsection{جریان ایجاد مشتری توسط سوپرادمین}
\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=1.1cm]
  \node[smallbox] (super) {Super Admin};
  \node[smallbox, right=1.1cm of super] (panel) {Next.js Panel};
  \node[smallbox, right=1.1cm of panel] (api) {\code{customer-create.php}};
  \node[smallbox, right=1.1cm of api] (auth) {Auth/Role};
  \node[smallbox, right=1.1cm of auth] (db) {MySQL};

  \draw[line] (super) -- node[above, font=\scriptsize]{submit tenant/site/admin} (panel);
  \draw[line] (panel) -- node[above, font=\scriptsize]{POST + JWT} (api);
  \draw[line] (api) -- node[above, font=\scriptsize]{require\_auth, super\_admin} (auth);
  \draw[line] (api) -- node[above, font=\scriptsize]{validate plan/email/domain} (db);
  \draw[line] (api) to[bend left=18] node[above, font=\scriptsize]{begin transaction} (db);
  \draw[line] (api) to[bend left=10] node[below, font=\scriptsize]{insert tenant, site, user, access} (db);
  \draw[line] (db) -- node[below, font=\scriptsize]{ids + commit} (api);
  \draw[line] (api) -- node[below, font=\scriptsize]{site\_key + install code} (panel);
\end{tikzpicture}
\end{adjustbox}
\caption{Sequence ایجاد مشتری، سایت و ادمین مشتری}
\end{figure}

\subsection{جریان مکالمه بازدیدکننده و پاسخ خودکار AI}
\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=0.9cm]
  \node[smallbox] (visitor) {Visitor};
  \node[smallbox, right=0.9cm of visitor] (widget) {Widget};
  \node[smallbox, right=0.9cm of widget] (wapi) {Widget API};
  \node[smallbox, right=0.9cm of wapi] (core) {Core Checks};
  \node[smallbox, right=0.9cm of core] (ai) {AI Engine};
  \node[smallbox, right=0.9cm of ai] (db) {MySQL};

  \draw[line] (visitor) -- node[above, font=\scriptsize]{open chat} (widget);
  \draw[line] (widget) -- node[above, font=\scriptsize]{config(site\_key)} (wapi);
  \draw[line] (widget) -- node[above, font=\scriptsize]{visitor-start} (wapi);
  \draw[line] (widget) -- node[above, font=\scriptsize]{conversation-start} (wapi);
  \draw[line] (wapi) -- node[above, font=\scriptsize]{origin + plan limit} (core);
  \draw[line] (core) -- node[above, font=\scriptsize]{read/write visitor, conversation} (db);
  \draw[line] (visitor) -- node[below, font=\scriptsize]{send message} (widget);
  \draw[line] (widget) -- node[below, font=\scriptsize]{message-send} (wapi);
  \draw[line] (wapi) -- node[below, font=\scriptsize]{insert message} (db);
  \draw[line] (widget) -- node[above, font=\scriptsize]{ai-reply(message\_id)} (wapi);
  \draw[line] (wapi) -- node[above, font=\scriptsize]{site/tenant/online/settings} (core);
  \draw[line] (core) -- node[above, font=\scriptsize]{find best answer} (ai);
  \draw[line] (ai) -- node[above, font=\scriptsize]{knowledge search} (db);
  \draw[line] (wapi) -- node[below, font=\scriptsize]{insert AI/fallback + log} (db);
  \draw[line] (widget) -- node[below, font=\scriptsize]{poll messages} (wapi);
\end{tikzpicture}
\end{adjustbox}
\caption{Sequence مکالمه و پاسخ خودکار AI در ویجت}
\end{figure}

\subsection{جریان پیشنهاد پاسخ برای Agent}
\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=1cm]
  \node[smallbox] (agent) {Agent};
  \node[smallbox, right=1cm of agent] (panel) {Panel};
  \node[smallbox, right=1cm of panel] (api) {\code{ai-suggestion-generate.php}};
  \node[smallbox, right=1cm of api] (access) {Auth + Site Access + Plan};
  \node[smallbox, right=1cm of access] (ai) {AI Answer Engine};
  \node[smallbox, right=1cm of ai] (db) {MySQL};

  \draw[line] (agent) -- node[above, font=\scriptsize]{request suggestion} (panel);
  \draw[line] (panel) -- node[above, font=\scriptsize]{POST conversation\_id + JWT} (api);
  \draw[line] (api) -- node[above, font=\scriptsize]{role, plan, site access} (access);
  \draw[line] (api) -- node[above, font=\scriptsize]{load latest visitor message} (db);
  \draw[line] (api) -- node[above, font=\scriptsize]{question + site} (ai);
  \draw[line] (ai) -- node[above, font=\scriptsize]{knowledge/chunks/questions} (db);
  \draw[line] (api) -- node[below, font=\scriptsize]{upsert ai\_suggestions + log} (db);
  \draw[line] (api) -- node[below, font=\scriptsize]{reply, confidence, sources} (panel);
\end{tikzpicture}
\end{adjustbox}
\caption{Sequence تولید پیشنهاد پاسخ برای اپراتور}
\end{figure}

\section{Class / Domain Diagram}
در PHP فعلی، مدل‌ها به‌صورت کلاس‌های domain entity پیاده‌سازی نشده‌اند؛ اما domain model سیستم از جداول و endpointها قابل استخراج است. نمودار زیر کلاس/موجودیت‌های دامنه و روابط اصلی را نشان می‌دهد.

\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=1.2cm and 1cm]
  \node[data] (tenant) {Tenant\\مشتری};
  \node[data, right=of tenant] (plan) {Plan\\پلن};
  \node[data, below=of tenant] (site) {Site\\سایت};
  \node[data, below=of site] (visitor) {Visitor\\بازدیدکننده};
  \node[data, right=of site] (user) {User\\کاربر پنل};
  \node[data, right=of user] (access) {AgentSiteAccess\\دسترسی سایت};
  \node[data, below=of visitor] (conv) {Conversation\\مکالمه};
  \node[data, right=of conv] (msg) {Message\\پیام};
  \node[data, right=of msg] (attach) {Attachment\\پیوست};
  \node[data, below=of site] at ($(site)+(3.2,-2.7)$) (knowledge) {KnowledgeSource\\دانش دستی};
  \node[data, below=of knowledge] (aitables) {AI Tables\\Pages, Chunks, Terms\\Questions, Logs};

  \draw[line] (tenant) -- node[above, font=\scriptsize]{has plan} (plan);
  \draw[line] (tenant) -- node[right, font=\scriptsize]{owns} (site);
  \draw[line] (tenant) -- node[above, font=\scriptsize]{has users} (user);
  \draw[line] (user) -- (access);
  \draw[line] (access) -- (site);
  \draw[line] (site) -- node[right, font=\scriptsize]{has visitors} (visitor);
  \draw[line] (visitor) -- node[right, font=\scriptsize]{starts} (conv);
  \draw[line] (site) -- (conv);
  \draw[line] (conv) -- node[above, font=\scriptsize]{contains} (msg);
  \draw[line] (msg) -- node[above, font=\scriptsize]{may have} (attach);
  \draw[line] (site) -- (knowledge);
  \draw[line] (site) -- (aitables);
  \draw[line] (knowledge) -- (aitables);
\end{tikzpicture}
\end{adjustbox}
\caption{نمودار دامنه و روابط مفهومی}
\end{figure}

\section{Microservice Diagram / نقشه سرویس‌های منطقی}
کد فعلی به‌صورت microservice فیزیکی جداگانه پیاده‌سازی نشده است؛ همه APIها در یک بک‌اند PHP قرار دارند. با این حال، معماری داخلی به چند سرویس منطقی تقسیم می‌شود که در آینده می‌توانند به سرویس مستقل تبدیل شوند.

\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=1cm and 1cm]
  \node[api] (gateway) {PHP API Backend\\Monolith فعلی};
  \node[smallbox, below left=of gateway] (auth) {Auth Service\\JWT, Roles};
  \node[smallbox, below=of gateway] (tenant) {Tenant Service\\Plans, Sites, Users};
  \node[smallbox, below right=of gateway] (chat) {Chat Service\\Visitors, Conversations};
  \node[smallbox, below=of tenant] (ai) {AI Knowledge Service\\Crawler, Search, Scoring};
  \node[smallbox, left=of ai] (file) {File Service\\Upload Validation};
  \node[smallbox, right=of ai] (report) {Reporting Service\\Dashboards, Usage};
  \node[data, below=of ai] (db) {Shared MySQL Database};

  \draw[line] (gateway) -- (auth);
  \draw[line] (gateway) -- (tenant);
  \draw[line] (gateway) -- (chat);
  \draw[line] (gateway) -- (file);
  \draw[line] (gateway) -- (ai);
  \draw[line] (gateway) -- (report);
  \draw[line] (auth) -- (db);
  \draw[line] (tenant) -- (db);
  \draw[line] (chat) -- (db);
  \draw[line] (file) -- (db);
  \draw[line] (ai) -- (db);
  \draw[line] (report) -- (db);
\end{tikzpicture}
\end{adjustbox}
\caption{نقشه سرویس‌های منطقی در معماری فعلی}
\end{figure}

\section{Network Diagram}
\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=1.3cm and 1.5cm]
  \node[actor] (internet) {Internet\\Browsers};
  \node[app, right=of internet] (edge) {HTTPS / Domain\\TLS Termination};
  \node[app, right=of edge] (frontend) {Frontend Host\\Next.js};
  \node[api, below=of frontend] (api) {Backend API Host\\PHP};
  \node[data, right=of api] (db) {Private DB Network\\MySQL};
  \node[data, below=of api] (storage) {Private/Protected Storage\\Uploads};
  \node[data, above=of frontend] (widget) {Static Widget URL\\CDN or Public Path};

  \draw[line] (internet) -- node[above, font=\scriptsize]{HTTPS} (edge);
  \draw[line] (edge) -- node[above, font=\scriptsize]{panel traffic} (frontend);
  \draw[line] (edge) |- node[left, font=\scriptsize]{api traffic} (api);
  \draw[line] (edge) |- node[left, font=\scriptsize]{widget script} (widget);
  \draw[line] (frontend) -- node[right, font=\scriptsize]{API requests} (api);
  \draw[line] (api) -- node[above, font=\scriptsize]{3306 / internal} (db);
  \draw[line] (api) -- node[right, font=\scriptsize]{file IO} (storage);
\end{tikzpicture}
\end{adjustbox}
\caption{نمودار شبکه پیشنهادی برای استقرار production}
\end{figure}

\subsection{مرزهای امنیتی شبکه}
\begin{itemize}
  \item دیتابیس نباید مستقیم از اینترنت قابل دسترس باشد و فقط backend مجوز اتصال داشته باشد.
  \item مسیرهای upload باید اجرای اسکریپت را مسدود کنند و فقط فایل‌های مجاز public شوند.
  \item دامنه frontend و backend باید در CORS و origin validation به‌صورت دقیق تنظیم شوند.
  \item ویجت باید از مسیر پایدار HTTPS ارائه شود تا سایت مشتری بتواند script را امن بارگذاری کند.
\end{itemize}

\section{Database Diagram}
\begin{figure}[h!]
\centering
\begin{adjustbox}{max width=\textwidth}
\begin{tikzpicture}[node distance=1cm and 0.9cm]
  \node[data] (plans) {plans};
  \node[data, below=of plans] (tenants) {tenants};
  \node[data, below=of tenants] (sites) {sites};
  \node[data, left=of sites] (users) {users};
  \node[data, left=of users] (asa) {agent\_site\_access};
  \node[data, below=of sites] (visitors) {visitors};
  \node[data, below=of visitors] (conv) {conversations};
  \node[data, right=of conv] (messages) {messages};
  \node[data, right=of messages] (attachments) {message\_attachments};
  \node[data, right=of sites] (knowledge) {knowledge\_sources};
  \node[data, below=of knowledge] (aisettings) {ai\_site\_settings};
  \node[data, below=of aisettings] (aipages) {ai\_pages};
  \node[data, right=of aipages] (chunks) {ai\_content\_chunks};
  \node[data, right=of chunks] (terms) {ai\_terms};
  \node[data, below=of chunks] (questions) {ai\_generated\_questions};
  \node[data, below=of messages] (logs) {ai\_answer\_logs};
  \node[data, below=of conv] (unanswered) {ai\_unanswered\_questions};

  \draw[line] (tenants) -- (plans);
  \draw[line] (tenants) -- (sites);
  \draw[line] (tenants) -- (users);
  \draw[line] (users) -- (asa);
  \draw[line] (asa) -- (sites);
  \draw[line] (sites) -- (visitors);
  \draw[line] (sites) -- (conv);
  \draw[line] (visitors) -- (conv);
  \draw[line] (conv) -- (messages);
  \draw[line] (messages) -- (attachments);
  \draw[line] (sites) -- (knowledge);
  \draw[line] (sites) -- (aisettings);
  \draw[line] (sites) -- (aipages);
  \draw[line] (aipages) -- (chunks);
  \draw[line] (chunks) -- (terms);
  \draw[line] (chunks) -- (questions);
  \draw[line] (messages) -- (logs);
  \draw[line] (conv) -- (logs);
  \draw[line] (conv) -- (unanswered);
  \draw[line] (messages) -- (unanswered);
\end{tikzpicture}
\end{adjustbox}
\caption{نمودار سطح بالا از پایگاه داده و روابط اصلی}
\end{figure}

\subsection{توضیح مدل داده}
مدل داده حول محور tenant و site شکل گرفته است. این دو موجودیت مرز اصلی چندمستاجری هستند. کاربران، سایت‌ها، دانش AI، مکالمات، پیام‌ها و گزارش‌ها باید در سطح tenant/site فیلتر شوند. جداول AI در migration موجود با کلیدهای خارجی به \code{tenants} و \code{sites} متصل شده‌اند و برای جست‌وجوی بهتر دارای ایندکس و \en{FULLTEXT KEY} هستند.

\section{معماری جریان داده}
\subsection{جریان داده چت}
\begin{enumerate}
  \item visitor در ویجت، پیام یا فایل ارسال می‌کند.
  \item API عمومی با \code{site\_key} سایت را پیدا می‌کند.
  \item origin و وضعیت tenant/site بررسی می‌شود.
  \item visitor و conversation در دیتابیس ثبت یا بازیابی می‌شوند.
  \item message در جدول \code{messages} ثبت می‌شود.
  \item agent از پنل، پیام‌های conversation را واکشی می‌کند.
  \item پاسخ agent در همان conversation ثبت می‌شود و ویجت در polling بعدی آن را دریافت می‌کند.
\end{enumerate}

\subsection{جریان داده دانش AI}
\begin{enumerate}
  \item customer admin منبع crawl یا دانش دستی تعریف می‌کند.
  \item crawler صفحه را دریافت و محتوای خوانا را استخراج می‌کند.
  \item متن در \code{ai\_pages} ذخیره و به chunk تبدیل می‌شود.
  \item از chunkها terms و generated questions ساخته می‌شود.
  \item هنگام سوال visitor، موتور AI روی knowledge، generated questions و chunks جست‌وجو می‌کند.
  \item پاسخ، confidence و sources در \code{ai\_answer\_logs} ثبت می‌شود.
  \item سوالات کم‌اعتماد وارد \code{ai\_unanswered\_questions} می‌شوند.
\end{enumerate}

\section{تصمیم‌های معماری}
\begin{longtable}{>{\raggedleft\arraybackslash}p{4.2cm} p{6cm} p{4.2cm}}
\toprule
\textbf{تصمیم} & \textbf{دلیل} & \textbf{اثر معماری} \\
\midrule
بک‌اند PHP فایل‌محور & توسعه سریع MVP و endpointهای قابل فهم & سادگی بالا، نیاز به نظم مستندات در رشد آینده \\
چندمستاجری با tenant/site & فروش SaaS به چند مشتری و چند سایت & نیاز دائمی به access control در همه endpointها \\
ویجت با Shadow DOM & جلوگیری از تداخل CSS سایت مشتری & نصب ساده و پایداری UI در سایت‌های مختلف \\
AI استخراجی مبتنی بر دانش & کنترل پاسخ، کاهش hallucination و کاهش وابستگی خارجی & نیاز به crawler، scoring، logs و unanswered loop \\
Plan Limits در backend & مدل درآمدی tiered و کنترل مصرف & enforcement در conversation، agent، site و AI features \\
JWT با token version & احراز هویت stateless همراه با امکان revoke & logout-all و تغییر رمز قابل اعمال می‌شود \\
\bottomrule
\end{longtable}

\section{مقیاس‌پذیری و مسیر تکامل معماری}
معماری فعلی برای MVP و demo فنی مناسب است و مرزبندی ماژول‌ها به‌خوبی قابل مشاهده است. برای رشد تجاری، پیشنهادهای زیر مسیر تکامل طبیعی معماری هستند:
\begin{itemize}
  \item انتقال crawlهای بزرگ از request همزمان به job queue و worker.
  \item افزودن API documentation رسمی برای endpointها.
  \item تفکیک storage عمومی و خصوصی، همراه با signed URL در صورت نیاز.
  \item اضافه کردن cache برای تنظیمات widget و plan usage.
  \item افزودن logging ساختاریافته، monitoring و alerting.
  \item تکمیل migrationهای schema پایه و seedهای demo.
  \item ایجاد تست integration برای جریان‌های tenant/site/access و widget.
\end{itemize}

\section{ریسک‌ها و محدودیت‌های معماری فعلی}
\begin{longtable}{>{\raggedleft\arraybackslash}p{4cm} p{10.5cm}}
\toprule
\textbf{ریسک/محدودیت} & \textbf{تحلیل و اقدام پیشنهادی} \\
\midrule
نبود router مرکزی & endpointهای مستقل ساده‌اند، اما در رشد پروژه باید naming، error handling و includeها استانداردتر شوند. \\
نبود migration کامل schema پایه & برای استقرار و ارزیابی فنی باید schema کامل جداول اصلی کنار migration AI ارائه شود. \\
Crawler همزمان & crawl در request می‌تواند timeout شود؛ worker و queue پیشنهاد می‌شود. \\
SSL verification در crawler & در کد فعلی برای fetch صفحات غیرفعال است؛ در production باید سیاست امن‌تری اعمال شود. \\
AI غیرمولد در جریان فعال & مزیت کنترل‌پذیری دارد، اما باید در ارائه با عنوان موتور دانش استخراجی معرفی شود نه LLM مستقل کامل. \\
تست خودکار محدود & برای بلوغ فنی، تست‌های auth، access، widget flow، upload و AI flow باید اضافه شوند. \\
\bottomrule
\end{longtable}

\section{جمع‌بندی}
معماری \en{AI Chat SaaS} از سه لایه client، API و داده تشکیل شده و برای یک محصول SaaS چندمستاجری طراحی شده است. مرزهای اصلی معماری شامل tenant، site، role، plan و knowledge هستند. ویجت چت، پنل مدیریتی و بک‌اند PHP به‌همراه MySQL و موتور AI استخراجی، یک چرخه کامل پشتیبانی انسانی و هوشمند ایجاد می‌کنند.

نکات قابل تاکید در ارزیابی:
\begin{itemize}
  \item معماری چندمستاجری واقعی و قابل فروش به چند مشتری.
  \item جداسازی نقش‌ها و کنترل دسترسی در سطح سایت.
  \item ترکیب چت انسانی، پاسخ خودکار، پیشنهاد AI و حلقه بهبود دانش.
  \item وجود ساختار plan و limit enforcement برای مدل SaaS.
  \item طراحی ویجت قابل نصب با Shadow DOM و API عمومی کنترل‌شده.
  \item مسیر تکامل روشن برای queue، monitoring، تست، billing و مستندسازی API.
\end{itemize}

\end{document}
